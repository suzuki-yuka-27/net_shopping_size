<% content_for(:title, OnePiece.model_name.human) %>
<div>
  <div class= "flex justify-center mb-10">
    <%= image_tag 'one_pieces_logo_image.png', size: '400x400' %>
  </div>
  <div class= "flex flex-row-reverse">
    <div class= "bg-black text-white text-center rounded-full w-20 px-4 py-2 mb-5">
      <%= link_to 'New', new_one_piece_path %>
    </div>
  </div>
  <div class= "flex justify-center">
    <% if @one_pieces.present? %>
      <div class= "grid grid-cols-1 lg:grid-cols-3 gap-7">
        <%= render @one_pieces %>
      </div>
    <% else %>
      <div class= "font-bold">
        <%= t('defaults.no_size') %>
      </div>
    <% end %>
  </div>
</div>